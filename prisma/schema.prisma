generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//for test db connection 
model User {
  id     Int     @id @default(autoincrement())
  username    String
  password    String 
}
